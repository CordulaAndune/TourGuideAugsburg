<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_item_height"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/place_image"
        android:layout_width="@dimen/list_item_height"
        android:layout_height="match_parent"
        android:contentDescription="@string/place_image"
        android:scaleType="centerCrop"
        tools:src="@drawable/augsburg_default" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginLeft="@dimen/outer_padding"
        android:layout_marginStart="@dimen/outer_padding"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/name"
            style="@style/ListItemStyle.Item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="@string/perlach_tower" />

        <TextView
            android:id="@+id/distance"
            style="@style/ListItemStyle.SubItem"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="@string/inner_city" />
    </LinearLayout>
</LinearLayout>